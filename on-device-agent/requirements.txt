# ProofWork On-Device Agent (실제 구동 버전)
# Windows 환경 전용

# === Core ===
psutil>=5.9.0
python-dotenv>=1.0.0
pydantic>=2.6.0
structlog>=24.1.0
schedule>=1.2.0

# === Windows 활성 창 추적 ===
pywin32>=306

# === 스크린 캡처 (선택) ===
mss>=9.0.0
Pillow>=10.2.0

# === Supabase (REST API via requests) ===
requests>=2.31.0

# === AI 화면 분석 (Gemini Vision) ===
google-generativeai>=0.8.0

# === 로컬 HTTP 서버 (server.py) ===
flask>=3.0.0
flask-cors>=4.0.0

# === 시스템 트레이 아이콘 (선택) ===
pystray>=0.19.0

# === 실행파일 빌드 (개발용) ===
# PyInstaller는 빌드 시에만 필요 (배포 패키지에는 불필요)
# pip install pyinstaller
